<!-- GENERATED by scripts/csf.py sync. Edit registry/examples.json, not this file. -->

# Linear Algebra Example Registry

| Example ID | Signature | Domain | First lesson | Reuse purpose |
|---|---|---|---|---|
| `LA-EX-001` | Represent the same two-component quantity as an arrow, an ordered pair, and a point in a simple data table | multiple representations | LA-0001 | — |
| `LA-EX-002` | Combine two simple ingredient vectors to show how linear combination changes several quantities at once | linear combination intuition | LA-0001 | — |
| `LA-EX-003` | Compare a rotation-like rule with a translation rule to test the intuition behind linear transformations | linearity boundary | LA-0001 | — |
| `LA-EX-004` | Represent a three-measurement sensor state as an ordered tuple and identify each scalar coordinate by index, meaning, and unit | coordinates and semantic ordering | LA-0002 | — |
| `LA-EX-005` | Decode `B in R^(4x6)` and indexed entries such as `b_3,5` before seeing any numerical matrix values | matrix shape and indexed notation | LA-0002 | — |
| `LA-EX-006` | Separate an underlying vector from its coordinate representation using the idea that a later basis change can alter coordinates without changing the represented object | object versus representation | LA-0002 | — |
| `LA-EX-007` | Use the same coordinate pair as a geometric displacement and as a point location to separate numerical representation from semantic role | vector role and geometric interpretation | LA-0003 | — |
| `LA-EX-008` | Represent a student or measurement record as an ordered feature vector and expose a silent coordinate-order contract bug that shape checking cannot detect | data-vector semantics | LA-0003 | — |
| `LA-EX-009` | Design a small room or process state vector, label units and omitted variables, and explain why system state is model-relative | state-vector modeling | LA-0003 | — |
| `LA-EX-010` | Combine two planar displacement vectors geometrically and coordinate-wise, then verify the endpoint agrees with head-to-tail addition. | vector addition geometry | LA-0004 | Connect coordinate addition to sequential displacement and commutativity. |
| `LA-EX-011` | Scale one system-change vector by positive, zero, and negative scalars to separate resizing, nullification, and reversal. | scalar multiplication semantics | LA-0004 | Build geometric and state-change intuition for scalar multiplication and additive inverse. |
| `LA-EX-012` | Compare algebraically compatible coordinate vectors whose real-world units or state/change semantics make addition or scaling questionable. | modeling contract | LA-0004 | Separate algebraic validity from semantic validity in vector models. |
| `LA-EX-013` | Build two different net robot displacements from the same pair of movement primitives using positive and negative scalar coefficients | geometric linear combinations | LA-0005 | — |
| `LA-EX-014` | Form a nonnegative sum-to-one mixture of two sensor signatures and separate algebraic validity from the physical assumption of approximately additive sensor response | weighted data mixtures | LA-0005 | — |
| `LA-EX-015` | Contrast an average-like nonnegative sum-to-one mixture with a generic signed linear combination to show that linear-combination coefficients need not be probabilities | coefficient constraints | LA-0005 | — |
| `LA-EX-016` | Solve a target-building coefficient problem, then compare it with parallel generators whose combinations remain on one line as preparation for span | target reachability and span preview | LA-0005 | — |
| `LA-EX-017` | Test three planar targets against the span of one nonzero generator and separate reachable scalar multiples from off-line targets | one-generator span membership | LA-0006 | — |
| `LA-EX-018` | Compare two pairs of planar generators—one parallel and one nonparallel—to show how equal generator counts can produce a line versus the whole plane | geometric span classification | LA-0006 | — |
| `LA-EX-019` | Decide target membership in a two-generator span by solving the coefficient equations and verifying the reconstructed target | span membership as solvability | LA-0006 | — |
| `LA-EX-020` | Add a third signal-pattern generator equal to the sum of two existing generators to show that redundancy can preserve the span while destroying coefficient uniqueness | span redundancy and signal dictionaries | LA-0006 | — |
| `LA-EX-021` | Interpret a two-variable budget equation as a line of allowed states and separate the algebraic solution set from application-specific integer or nonnegative restrictions | linear equation as constraint | LA-0007 | — |
| `LA-EX-022` | Interpret one three-variable linear equality as a plane of valid states and show how extra nonnegativity conditions restrict the physically feasible subset | three-variable constraint geometry | LA-0007 | — |
| `LA-EX-023` | Read the same linear equation as geometric line, budget relation, and fixed sensor measurement to separate algebraic structure from domain semantics | multiple interpretations of one constraint | LA-0007 | — |
| `LA-EX-024` | Preview a two-equation system as intersection of two line constraints and solve one intersection point to prepare for solution-set classification | constraint intersection preview | LA-0007 | — |
| `LA-EX-025` | Classify three two-variable systems as one solution, no solution, or infinitely many solutions by comparing nonparallel, incompatible parallel, and coincident line constraints | linear-system solution classification | LA-0008 | — |
| `LA-EX-026` | Solve a two-plane three-variable system into a one-parameter line and write the solution as one particular point plus a spanning direction | parametric solution set in R3 | LA-0008 | — |
| `LA-EX-027` | Convert a target-vector span-membership equation into a scalar coefficient system and interpret system consistency as target reachability | span membership as system consistency | LA-0008 | — |
| `LA-EX-028` | Compare two three-equation two-unknown systems to show that overdetermined equation count can be compatible or contradictory depending on whether all constraints share a common solution | overdetermined consistency | LA-0008 | — |
| `LA-EX-029` | Convert a two-equation two-unknown system into coefficient matrix, unknown vector, right-hand-side vector, and future Ax=b representation | system-to-matrix organization | LA-0009 | — |
| `LA-EX-030` | Interpret a 2-by-3 matrix as an operator interface that expects three input coordinates and produces two output coordinates, using shape as an input-output contract | matrix shape as operator contract | LA-0009 | — |
| `LA-EX-031` | Give the same 3-by-2 numeric array two distinct meanings—three observations by two features versus an operator from two-coordinate inputs to three-coordinate outputs—to separate matrix entries from matrix semantics | matrix as data versus operator | LA-0009 | — |
| `LA-EX-032` | Interpret a 2-by-4 neural-decoder weight matrix as four neural input features mapped to two decoded outputs while explicitly separating engineered linear weights from biological synaptic strengths or causal claims | matrix operator in neural decoding | LA-0009 | — |
| `LA-EX-033` | Compute a 2-by-3 matrix-vector product explicitly as a weighted sum of the matrix columns and verify that the output dimension follows the matrix row count | matrix-vector multiplication by columns | LA-0010 | — |
| `LA-EX-034` | Rewrite one matrix equation Ax=b as both a column-span reachability problem and the equivalent scalar system of equations | matrix equation as span and system | LA-0010 | — |
| `LA-EX-035` | Interpret a 2-by-4 neural-decoder matrix by columns, showing how each neural feature contributes a two-output response pattern without equating decoder coefficients to biological synapses | matrix-vector multiplication in neural decoding | LA-0010 | — |
| `LA-EX-036` | Mix two modeled source amplitudes into three sensor outputs using a 3-by-2 matrix to interpret each column as one source signature across all sensors | linear source mixing as column combination | LA-0010 | — |
| `LA-EX-037` | Solve a two-equation system with a unique solution by replacing one row with itself minus a multiple of another, then back-substitute from the resulting triangular structure | elimination with unique solution | LA-0011 | — |
| `LA-EX-038` | Eliminate proportional left-hand sides with incompatible right-hand sides to expose a contradiction row of the form zero equals a nonzero constant | elimination exposing inconsistency | LA-0011 | — |
| `LA-EX-039` | Eliminate one equation that is an exact scalar multiple of another to expose a zero-equals-zero row and interpret it as redundant information rather than inconsistency | elimination exposing redundancy | LA-0011 | — |
| `LA-EX-040` | Solve a two-parameter sensor calibration system for gain and offset by subtracting one calibration constraint from another, transferring elimination to an engineering setting | elimination in calibration | LA-0011 | — |
| `LA-EX-041` | Compare three echelon-form systems with the same three variables to classify unique, one-parameter, and inconsistent solution structures from pivots, free variables, and contradiction rows | pivot and free-variable classification | LA-0012 | — |
| `LA-EX-042` | Parameterize a consistent three-variable system with one free variable and rewrite the solution as one particular point plus a direction vector to expose line geometry | one-free-variable solution geometry | LA-0012 | — |
| `LA-EX-043` | Parameterize one linear equation in three variables with two free parameters and write the full solution as a particular point plus two independent directions | two-free-variable solution geometry | LA-0012 | — |
| `LA-EX-044` | Interpret a three-parameter sensor calibration model with only two independent constraints as a one-parameter family, showing how a free variable represents structural non-identifiability | free variables in engineering calibration | LA-0012 | — |
